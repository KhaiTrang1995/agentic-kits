# Pipeline: {{service}}-logs
# DRAFT — review before deploy. No secrets in this file.

input {
  # beats { port => 5044 }
}

filter {
  # json { source => "message" skip_on_invalid_json => true }

  # grok {
  #   match => { "message" => "%{TIMESTAMP_ISO8601:ts} %{LOGLEVEL:level} %{GREEDYDATA:msg}" }
  #   tag_on_failure => ["_grokparsefailure"]
  # }

  # date {
  #   match => [ "ts", "ISO8601" ]
  #   target => "@timestamp"
  # }

  mutate {
    add_field => {
      "[service][name]" => "{{service}}"
      "[event][dataset]" => "{{service}}.application"
    }
  }
}

output {
  # elasticsearch {
  #   hosts => ["${ES_HOSTS}"]
  #   index => "{{env}}-{{service}}-logs-%{+YYYY.MM.dd}"
  #   user => "${ES_USER}"
  #   password => "${ES_PASSWORD}"
  # }
}
