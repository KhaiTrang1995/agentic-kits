# LEARN (condensed)

1. Play demos under `demos/runner-kit-3d/`.
2. Read `kit/core.js` + `kit/spawner.js`.
3. Change only `CONFIG` in one game.
4. `/runner-new` from closest template.
5. Optional: custom leaderboard API (document only; localStorage default).

Full narrative guide ships with the engine demo README.
